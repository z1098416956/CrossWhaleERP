<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neton.dao.GoodsMultiAttributeDao">

    <select id="queryGoodsMultiAttributePage" resultType="com.neton.res.GoodsMultiAttributeResVO">
        SELECT 
            id,
            attribute_name AS attributeName,
            attribute_value AS attributeValue
        FROM 
            goods_multi_attribute
        WHERE 
            is_deleted = 0
        <if test="reqVO.attributeName != null and reqVO.attributeName != ''">
            AND attribute_name LIKE CONCAT('%', #{reqVO.attributeName}, '%')
        </if>
        <if test="reqVO.attributeValue != null and reqVO.attributeValue != ''">
            AND attribute_value LIKE CONCAT('%', #{reqVO.attributeValue}, '%')
        </if>
        ORDER BY 
            create_time DESC
    </select>
</mapper>
